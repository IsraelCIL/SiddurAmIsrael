name: sefaria_importer
description: Standalone CLI tool to fetch Mincha prayer texts from the Sefaria API with multi-nusach support.
version: 1.0.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  http: ^1.2.0
  args: ^2.5.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
  mocktail: ^1.0.4
